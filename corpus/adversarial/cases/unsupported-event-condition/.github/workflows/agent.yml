name: unsupported-event-condition
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  agent:
    if: startsWith(github.event_name, 'pull')
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@0123456789abcdef0123456789abcdef01234567
