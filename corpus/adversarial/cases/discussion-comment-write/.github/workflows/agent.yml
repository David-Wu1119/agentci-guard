name: discussion-comment-write
on: discussion_comment
permissions:
  discussions: write
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@0123456789abcdef0123456789abcdef01234567
        with:
          prompt: ${{ github.event.comment.body }}
