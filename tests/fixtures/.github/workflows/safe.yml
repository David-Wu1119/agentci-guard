name: safe-ai-agent
on:
  pull_request:

permissions:
  contents: read

jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run read-only static analysis
        run: |
          echo "Summarize diff metadata generated by a trusted preprocessor"
